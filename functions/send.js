export async function onRequestPost(context){try{const formData=await context.request.formData();const content={name:formData.get("name"),email:formData.get("email"),message:formData.get("message"),subject:`Contact Form Submission from ${formData.get("name")}`};console.log({message:"Request Received",context:context,content:content});const email={to:"erinlbrodrick@icloud.com",from:"hello@maclong.uk",subject:content.subject||"New Contact Form Submission",text:content.message||"No message provided"};const response=await context.env.EMAIL.send(email);return new Response(JSON.stringify({success:true}),{status:200,headers:{"Content-Type":"application/json"}})}catch(error){console.error("Error sending email:",error);return new Response(JSON.stringify({error:"Failed to send email"}),{status:500,headers:{"Content-Type":"application/json"}})}}
